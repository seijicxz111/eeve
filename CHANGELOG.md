# eeve

## 0.2.0

Renamed the public package to `eeve`, added the MVP showcase site, added the Eeve moon icon, and fixed polymorphic `Button` rendering with `asChild`.

## 0.1.0

Initial package release as `eeve-ui-components` with tokens, components, Storybook, tests, package build configuration, and release documentation.
